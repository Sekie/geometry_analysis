name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Dependencies -- Added in
  - numpy

    # Pip-only installs
  #- pip:
  #  - codecov

